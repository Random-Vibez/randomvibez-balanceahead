{
  "name": "balanceahead",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check app-core.js && node --check app.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  }
}
